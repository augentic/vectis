## App Reference

<!-- Which Crux app is this iOS shell for? Provide the path to the app directory.
     The app must already have a generated shared crate.
     Example: examples/todo -->

## Target Directory

<!-- Where the iOS shell will be generated.
     Example: examples/todo/iOS -->

## Motivation

<!-- Why this shell is being built. Testing? Production? Learning exercise? -->

## Design System Notes

<!-- Any customizations to the default VectisDesign tokens for this app.
     Leave empty to use the defaults from design-system/tokens.yaml.

     Example:
     - Override primary color to match app branding
     - Use title3 instead of headline for list items -->
